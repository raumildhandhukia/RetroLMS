describe('Instructor Router Testing', () => {
    test('POST /courses testing', () => {
        // Test logic for create a new course
    });
    
    test('POST /courses/:courseId/students testing', () => {
        // Test logic for add a new student to a course
    });
  
    test('POST /courses/:courseId/tasks testing', () => {
        // Test logic for add a new task to a course
    });
  
    test('POST /courses/:courseId/tasks/:taskId/points testing', () => {
      // Test logic for assign points to a student for a task
    });
  
    test('POST /shop/items testing', () => {
        // Test logic for create items in the shop
    });

})